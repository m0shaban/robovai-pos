<Page x:Class="SmartPOS.WPF.Views.ShiftManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      FlowDirection="RightToLeft"
      Loaded="Page_Loaded">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Background="{DynamicResource AlAtmani.BackgroundBrush}"
              TextElement.Foreground="{DynamicResource AlAtmani.TextPrimaryBrush}"
              Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:Card Grid.Row="0"
                             Style="{StaticResource AlAtmani.GlassCard}"
                             Margin="0,0,0,24"
                             Padding="24">
            <DockPanel>
                <Border DockPanel.Dock="Right" Background="{DynamicResource AlAtmani.Surface2Brush}"
                        CornerRadius="12"
                        Padding="12"
                        Margin="0,0,16,0"
                        VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="ClockOutline"
                                             Width="32"
                                             Height="32"
                                             Foreground="{DynamicResource AlAtmani.AccentBrush}"/>
                </Border>
                <StackPanel DockPanel.Dock="Right" VerticalAlignment="Center">
                    <TextBlock Text="إدارة الورديات"
                               Style="{StaticResource AlAtmani.HeaderText}"/>
                    <TextBlock Text="إدارة ومراقبة ورديات العمل والمبيعات"
                               FontSize="14"
                               Opacity="0.85"
                               Foreground="{DynamicResource AlAtmani.TextSecondaryBrush}"
                               Margin="0,4,0,0"/>
                </StackPanel>
                
                <!-- Toolbar -->
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button Command="{Binding LoadShiftsCommand}"
                            Style="{StaticResource MaterialDesignIconButton}"
                            ToolTip="تحديث البيانات"
                            Foreground="{DynamicResource AlAtmani.AccentBrush}">
                        <materialDesign:PackIcon Kind="Refresh" Width="24" Height="24"/>
                    </Button>
                </StackPanel>
            </DockPanel>
        </materialDesign:Card>

        <!-- Current Shift Status -->
        <materialDesign:Card Grid.Row="1"
                             Style="{StaticResource AlAtmani.GlassCard}"
                             Margin="0,0,0,24"
                             Padding="24">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="الوردية الحالية"
                               Style="{StaticResource AlAtmani.SubHeaderText}"
                               Margin="0,0,0,16"/>

                    <StackPanel Orientation="Horizontal"
                                Margin="0,5">
                        <materialDesign:PackIcon Kind="Account"
                                                 Width="20"
                                                 Height="20"
                                                 Foreground="{DynamicResource AlAtmani.AccentBrush}"
                                                 Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding CurrentShift.User.FullName, StringFormat=الكاشير: {0}}"
                                   FontSize="16"
                                   Opacity="0.85"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal"
                                Margin="0,5">
                        <materialDesign:PackIcon Kind="Clock"
                                                 Width="20"
                                                 Height="20"
                                                 Foreground="{DynamicResource AlAtmani.AccentBrush}"
                                                 Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding CurrentShift.StartTime, StringFormat=وقت البدء: {0:hh:mm tt}}"
                                   FontSize="16"
                                   Opacity="0.85"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal"
                                Margin="0,5">
                        <materialDesign:PackIcon Kind="CashMultiple"
                                                 Width="20"
                                                 Height="20"
                                                 Foreground="{DynamicResource AlAtmani.AccentBrush}"
                                                 Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding CurrentShift.OpeningBalance, StringFormat=رصيد الافتتاح: {0:F2} ج.م}"
                                   FontSize="16"
                                   Opacity="0.85"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal"
                                Margin="0,15,0,0">
                        <TextBox Width="150"
                                 Margin="0,0,10,0"
                                 Text="{Binding OpeningBalanceInput, UpdateSourceTrigger=PropertyChanged}"
                                 materialDesign:HintAssist.Hint="رصيد الافتتاح"/>
                        <TextBox Width="160"
                                 Margin="0,0,10,0"
                                 Text="{Binding ClosingBalanceInput, UpdateSourceTrigger=PropertyChanged}"
                                 materialDesign:HintAssist.Hint="النقدية في الدرج"/>
                        <TextBox Width="200"
                                 Text="{Binding NotesInput, UpdateSourceTrigger=PropertyChanged}"
                                 materialDesign:HintAssist.Hint="ملاحظات (سبب العجز/الزيادة)"/>
                        <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                                Height="40"
                                Margin="10,0,0,0"
                                Content="تحديث"
                                Command="{Binding LoadShiftsCommand}"
                                ToolTip="تحديث بيانات الوردية الحالية"/>
                    </StackPanel>

                    <!-- Expected Balance: ONLY visible to Admin/SuperAdmin (Blind Z-Report) -->
                    <Border Margin="0,12,0,0" Padding="8" CornerRadius="8"
                            Background="#1AEF4444">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsCashier}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ShieldLockOutline" Width="20" Height="20" Foreground="#EF4444" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding ExpectedBalance, StringFormat=الرصيد المتوقع (للإدارة فقط): {0:N2} ج.م}"
                                       FontSize="14" FontWeight="SemiBold" Foreground="#EF4444" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="1"
                            VerticalAlignment="Center">
                    <Button Style="{StaticResource AlAtmani.SoftButton}"
                            Height="54"
                            Padding="24,0"
                            Content="فتح وردية"
                            Margin="0,0,0,10"
                            Command="{Binding OpenShiftCommand}"
                            IsEnabled="{Binding IsShiftOpen, Converter={StaticResource BoolNegationConverter}}"/>
                    <Button Style="{StaticResource AlAtmani.SoftButton}"
                            Height="54"
                            Padding="24,0"
                            Content="إنهاء الوردية"
                            Command="{Binding CloseShiftCommand}"
                            IsEnabled="{Binding IsShiftOpen}"/>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- Shift Statistics -->
        <Grid Grid.Row="2">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsCashier}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Stat Card 1: Total Sales -->
            <materialDesign:Card Grid.Column="0"
                                 Grid.Row="0"
                                 Style="{StaticResource AlAtmani.GlassCard}"
                                 Margin="0,0,8,16"
                                 Padding="20">
                <StackPanel>
                    <Border Background="{DynamicResource AlAtmani.Surface2Brush}"
                            CornerRadius="12"
                            Padding="10"
                            HorizontalAlignment="Left"
                            Margin="0,0,0,12">
                        <materialDesign:PackIcon Kind="CashRegister"
                                                 Width="32"
                                                 Height="32"
                                                 Foreground="{DynamicResource AlAtmani.AccentBrush}"/>
                    </Border>
                    <TextBlock Text="إجمالي المبيعات"
                               FontSize="14"
                               Opacity="0.7"/>
                    <TextBlock Text="{Binding CurrentShiftTotalSales, StringFormat={}{0:N2} ج.م}"
                               FontSize="24"
                               FontWeight="Bold"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Stat Card 2: Transactions -->
            <materialDesign:Card Grid.Column="1"
                                 Grid.Row="0"
                                 Style="{StaticResource AlAtmani.GlassCard}"
                                 Margin="8,0,8,16"
                                 Padding="20">
                <StackPanel>
                    <Border Background="{DynamicResource AlAtmani.Surface2Brush}"
                            CornerRadius="12"
                            Padding="10"
                            HorizontalAlignment="Left"
                            Margin="0,0,0,12">
                        <materialDesign:PackIcon Kind="Receipt"
                                                 Width="32"
                                                 Height="32"
                                                 Foreground="{DynamicResource AlAtmani.AccentBrush}"/>
                    </Border>
                    <TextBlock Text="عدد المعاملات"
                               FontSize="14"
                               Opacity="0.7"/>
                    <TextBlock Text="{Binding CurrentShiftTransactions, StringFormat={}{0} معاملة}"
                               FontSize="24"
                               FontWeight="Bold"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Stat Card 3: Cash -->
            <materialDesign:Card Grid.Column="2"
                                 Grid.Row="0"
                                 Style="{StaticResource AlAtmani.GlassCard}"
                                 Margin="8,0,8,16"
                                 Padding="20">
                <StackPanel>
                    <Border Background="{DynamicResource AlAtmani.Surface2Brush}"
                            CornerRadius="12"
                            Padding="10"
                            HorizontalAlignment="Left"
                            Margin="0,0,0,12">
                        <materialDesign:PackIcon Kind="Cash"
                                                 Width="32"
                                                 Height="32"
                                                 Foreground="{DynamicResource AlAtmani.AccentBrush}"/>
                    </Border>
                    <TextBlock Text="النقدية"
                               FontSize="14"
                               Opacity="0.7"/>
                    <TextBlock Text="{Binding CurrentShiftSales, StringFormat={}{0:N2} ج.م}"
                               FontSize="24"
                               FontWeight="Bold"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Stat Card 4: Cards -->
            <materialDesign:Card Grid.Column="3"
                                 Grid.Row="0"
                                 Style="{StaticResource AlAtmani.GlassCard}"
                                 Margin="8,0,0,16"
                                 Padding="20">
                <StackPanel>
                    <Border Background="{DynamicResource AlAtmani.Surface2Brush}"
                            CornerRadius="12"
                            Padding="10"
                            HorizontalAlignment="Left"
                            Margin="0,0,0,12">
                        <materialDesign:PackIcon Kind="CreditCard"
                                                 Width="32"
                                                 Height="32"
                                                 Foreground="#C2185B"/>
                    </Border>
                    <TextBlock Text="البطاقات"
                               FontSize="14"
                               Opacity="0.7"/>
                    <TextBlock Text="{Binding TotalCardSales, StringFormat={}{0:N2} ج.م}"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="#C2185B"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Stat Card 5: Vodafone Cash -->
            <materialDesign:Card Grid.Column="0"
                                 Grid.Row="1"
                                 Style="{StaticResource AlAtmani.GlassCard}"
                                 Margin="0,0,8,16"
                                 Padding="20">
                <StackPanel>
                    <Border Background="{DynamicResource AlAtmani.Surface2Brush}"
                            CornerRadius="12"
                            Padding="10"
                            HorizontalAlignment="Left"
                            Margin="0,0,0,12">
                        <materialDesign:PackIcon Kind="CellphoneNfc"
                                                 Width="32"
                                                 Height="32"
                                                 Foreground="#E60000"/>
                    </Border>
                    <TextBlock Text="فودافون كاش"
                               FontSize="14"
                               Opacity="0.7"/>
                    <TextBlock Text="{Binding TotalVodafoneCashSales, StringFormat={}{0:N2} ج.م}"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="#E60000"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Stat Card 6: InstaPay -->
            <materialDesign:Card Grid.Column="1"
                                 Grid.Row="1"
                                 Style="{StaticResource AlAtmani.GlassCard}"
                                 Margin="8,0,8,16"
                                 Padding="20">
                <StackPanel>
                    <Border Background="{DynamicResource AlAtmani.Surface2Brush}"
                            CornerRadius="12"
                            Padding="10"
                            HorizontalAlignment="Left"
                            Margin="0,0,0,12">
                        <materialDesign:PackIcon Kind="BankTransfer"
                                                 Width="32"
                                                 Height="32"
                                                 Foreground="#1877F2"/>
                    </Border>
                    <TextBlock Text="انستا باي"
                               FontSize="14"
                               Opacity="0.7"/>
                    <TextBlock Text="{Binding TotalInstaPaySales, StringFormat={}{0:N2} ج.م}"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="#1877F2"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Stat Card 7: Deferred Sales (الآجل) -->
            <materialDesign:Card Grid.Column="2"
                                 Grid.Row="1"
                                 Style="{StaticResource AlAtmani.GlassCard}"
                                 Margin="8,0,8,16"
                                 Padding="20">
                <StackPanel>
                    <Border Background="{DynamicResource AlAtmani.Surface2Brush}"
                            CornerRadius="12"
                            Padding="10"
                            HorizontalAlignment="Left"
                            Margin="0,0,0,12">
                        <materialDesign:PackIcon Kind="BookAccountOutline"
                                                 Width="32"
                                                 Height="32"
                                                 Foreground="#D97706"/>
                    </Border>
                    <TextBlock Text="المبيعات الآجلة"
                               FontSize="14"
                               Opacity="0.7"/>
                    <TextBlock Text="{Binding TotalDeferredSales, StringFormat={}{0:N2} ج.م}"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="#D97706"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Recent Shifts List (Moved outside the restricted grid so Cashiers can see it) -->
        </Grid>

        <!-- Recent Shifts List -->
        <materialDesign:Card Grid.Row="3"
                             Style="{StaticResource AlAtmani.GlassCard}"
                             Margin="0"
                             Padding="24">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <DockPanel Grid.Row="0" Margin="0,0,0,16">
                        <TextBlock Text="الورديات السابقة"
                                   Style="{StaticResource AlAtmani.SubHeaderText}"
                                   VerticalAlignment="Center"/>
                        
                        <TextBox materialDesign:HintAssist.Hint="بحث بالكاشير أو رقم الوردية..."
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 Padding="8,6"
                                 Width="250"
                                 HorizontalAlignment="Left"
                                 materialDesign:TextFieldAssist.HasClearButton="True"
                                 VerticalAlignment="Center"/>
                    </DockPanel>

                    <DataGrid Grid.Row="1"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              Background="Transparent"
                              BorderThickness="0"
                              HeadersVisibility="Column"
                              FontSize="14"
                              ItemsSource="{Binding Shifts}"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              ScrollViewer.CanContentScroll="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="التاريخ"
                                                Binding="{Binding StartTime, StringFormat={}{0:yyyy-MM-dd}, Mode=OneWay}"
                                                Width="*"/>
                            <DataGridTextColumn Header="الكاشير"
                                                Binding="{Binding User.FullName, Mode=OneWay}"
                                                Width="*"/>
                            <DataGridTextColumn Header="وقت البدء"
                                                Binding="{Binding StartTime, StringFormat={}{0:hh:mm tt}, Mode=OneWay}"
                                                Width="*"/>
                            <DataGridTextColumn Header="وقت الإنهاء"
                                                Binding="{Binding EndTime, StringFormat={}{0:hh:mm tt}, Mode=OneWay}"
                                                Width="*"/>
                            <DataGridTextColumn Header="المبيعات"
                                                Binding="{Binding TotalSales, StringFormat={}{0:N2} ج.م, Mode=OneWay}"
                                                Width="*"/>
                            <DataGridTextColumn Header="رصيد الإغلاق"
                                                Binding="{Binding ClosingBalance, StringFormat={}{0:N2} ج.م, Mode=OneWay}"
                                                Width="*"/>
                            <DataGridTemplateColumn Header="العجز/الزيادة" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Difference, StringFormat={}{0:N2} ج.م}"
                                                   FontWeight="SemiBold"
                                                   VerticalAlignment="Center"
                                                   Foreground="{Binding Difference, Converter={StaticResource DifferenceToColorConverter}}">
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="المعاملات"
                                                Binding="{Binding TransactionCount, Mode=OneWay}"
                                                Width="*"/>
                            <DataGridTemplateColumn Header="الحالة" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Padding="12,4" CornerRadius="12" HorizontalAlignment="Left" VerticalAlignment="Center">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#F3F4F6"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Open">
                                                            <Setter Property="Background" Value="#DCFCE7"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Closed">
                                                            <Setter Property="Background" Value="#FEE2E2"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Reconciled">
                                                            <Setter Property="Background" Value="#E0E7FF"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock FontWeight="SemiBold" FontSize="12">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="#4B5563"/>
                                                        <Setter Property="Text" Value="غير محدد"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="Open">
                                                                <Setter Property="Foreground" Value="#15803D"/>
                                                                <Setter Property="Text" Value="مفتوحة"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Closed">
                                                                <Setter Property="Foreground" Value="#B91C1C"/>
                                                                <Setter Property="Text" Value="مغلقة"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Reconciled">
                                                                <Setter Property="Foreground" Value="#4338CA"/>
                                                                <Setter Property="Text" Value="مُسواة"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </materialDesign:Card>
        </Grid>
    </ScrollViewer>
</Page>
