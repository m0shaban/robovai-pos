<Page x:Class="SmartPOS.WPF.Views.ReturnsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      FlowDirection="RightToLeft"
      Loaded="Page_Loaded"
      Unloaded="Page_Unloaded">

    <Grid Background="{DynamicResource AlAtmani.BackgroundBrush}"
          TextElement.Foreground="{DynamicResource AlAtmani.TextPrimaryBrush}"
          Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:Card Grid.Row="0"
                             Style="{StaticResource AlAtmani.GlassCard}"
                             Margin="0,0,0,24"
                             Padding="24">
            <DockPanel>
                <Border DockPanel.Dock="Right" Background="{DynamicResource AlAtmani.Surface2Brush}"
                        CornerRadius="12"
                        Padding="12"
                        Margin="0,0,16,0"
                        VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="BackupRestore"
                                             Width="32"
                                             Height="32"
                                             Foreground="{DynamicResource AlAtmani.AccentBrush}"/>
                </Border>
                <StackPanel DockPanel.Dock="Right" VerticalAlignment="Center">
                    <TextBlock Text="نظام المرتجعات"
                               Style="{StaticResource AlAtmani.HeaderText}"/>
                    <TextBlock Text="إدارة مرتجعات المنتجات والاستردادات"
                               FontSize="14"
                               Opacity="0.85"
                               Foreground="{DynamicResource AlAtmani.TextSecondaryBrush}"
                               Margin="0,4,0,0"/>
                </StackPanel>
                
                <!-- Toolbar -->
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button Command="{Binding LoadCommand}"
                            Style="{StaticResource MaterialDesignIconButton}"
                            ToolTip="تحديث البيانات"
                            Foreground="{DynamicResource AlAtmani.AccentBrush}">
                        <materialDesign:PackIcon Kind="Refresh" Width="24" Height="24"/>
                    </Button>
                </StackPanel>
            </DockPanel>
        </materialDesign:Card>

        <!-- Statistics -->
        <Grid Grid.Row="1"
              Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Total Returns -->
            <materialDesign:Card Grid.Column="0"
                                 Style="{StaticResource AlAtmani.GlassCard}"
                                 Margin="0,0,8,0"
                                 Padding="20">
                <StackPanel>
                    <Border Background="{DynamicResource AlAtmani.Surface2Brush}"
                            CornerRadius="12"
                            Padding="10"
                            HorizontalAlignment="Left"
                            Margin="0,0,0,12">
                        <materialDesign:PackIcon Kind="ArrowULeftTop"
                                                 Width="32"
                                                 Height="32"
                                                 Foreground="{DynamicResource AlAtmani.AccentBrush}"/>
                    </Border>
                    <TextBlock Text="إجمالي المرتجعات"
                               FontSize="14"
                               Opacity="0.7"/>
                    <TextBlock Text="{Binding TotalReturnsCount, FallbackValue='0 مرتجع'}"
                               FontSize="24"
                               FontWeight="Bold"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Pending -->
            <materialDesign:Card Grid.Column="1"
                                 Style="{StaticResource AlAtmani.GlassCard}"
                                 Margin="8,0,8,0"
                                 Padding="20">
                <StackPanel>
                    <Border Background="{DynamicResource AlAtmani.Surface2Brush}"
                            CornerRadius="12"
                            Padding="10"
                            HorizontalAlignment="Left"
                            Margin="0,0,0,12">
                        <materialDesign:PackIcon Kind="ClockAlert"
                                                 Width="32"
                                                 Height="32"
                                                 Foreground="{DynamicResource AlAtmani.AccentBrush}"/>
                    </Border>
                    <TextBlock Text="قيد الانتظار"
                               FontSize="14"
                               Opacity="0.7"/>
                    <TextBlock Text="{Binding PendingReturnsCount, FallbackValue='0 مرتجع'}"
                               FontSize="24"
                               FontWeight="Bold"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Approved -->
            <materialDesign:Card Grid.Column="2"
                                 Style="{StaticResource AlAtmani.GlassCard}"
                                 Margin="8,0,8,0"
                                 Padding="20">
                <StackPanel>
                    <Border Background="{DynamicResource AlAtmani.Surface2Brush}"
                            CornerRadius="12"
                            Padding="10"
                            HorizontalAlignment="Left"
                            Margin="0,0,0,12">
                        <materialDesign:PackIcon Kind="CheckCircle"
                                                 Width="32"
                                                 Height="32"
                                                 Foreground="{DynamicResource AlAtmani.AccentBrush}"/>
                    </Border>
                    <TextBlock Text="تم القبول"
                               FontSize="14"
                               Opacity="0.7"/>
                    <TextBlock Text="{Binding ApprovedReturnsCount, FallbackValue='0 مرتجع'}"
                               FontSize="24"
                               FontWeight="Bold"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Total Amount -->
            <materialDesign:Card Grid.Column="3"
                                 Style="{StaticResource AlAtmani.GlassCard}"
                                 Margin="8,0,0,0"
                                 Padding="20">
                <StackPanel>
                    <Border Background="{DynamicResource AlAtmani.Surface2Brush}"
                            CornerRadius="12"
                            Padding="10"
                            HorizontalAlignment="Left"
                            Margin="0,0,0,12">
                        <materialDesign:PackIcon Kind="CashRefund"
                                                 Width="32"
                                                 Height="32"
                                                 Foreground="{DynamicResource AlAtmani.AccentBrush}"/>
                    </Border>
                    <TextBlock Text="قيمة المرتجعات"
                               FontSize="14"
                               Opacity="0.7"/>
                    <TextBlock Text="{Binding TotalReturnsAmount, StringFormat={}{0:N2} ج.م, FallbackValue='0.00 ج.م'}"
                               FontSize="24"
                               FontWeight="Bold"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>

        <!-- Returns List -->
        <materialDesign:Card Grid.Row="2"
                             Style="{StaticResource AlAtmani.GlassCard}"
                             Margin="0"
                             Padding="24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0"
                            Orientation="Horizontal"
                            Margin="0,0,0,15">
                    <TextBlock Text="قائمة المرتجعات"
                               Style="{StaticResource AlAtmani.SubHeaderText}"
                               Margin="0"/>

                    <Button Style="{StaticResource AlAtmani.SoftButton}"
                            Height="45"
                            Padding="20,0"
                            Content="مرتجع جديد"
                            Margin="20,0,0,0"/>
                </StackPanel>

                <!-- Create Return Form -->
                <Grid Grid.Row="1"
                      Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ComboBox Grid.Column="0"
                              ItemsSource="{Binding Sales}"
                              DisplayMemberPath="InvoiceNumber"
                              SelectedItem="{Binding SelectedSale}"
                              Margin="0,0,10,0"
                              materialDesign:HintAssist.Hint="اختر الفاتورة"/>

                    <ComboBox Grid.Column="1"
                              ItemsSource="{Binding Reasons}"
                              SelectedItem="{Binding SelectedReason}"
                              Margin="0,0,10,0"
                              materialDesign:HintAssist.Hint="سبب المرتجع"/>

                    <TextBox Grid.Column="2"
                             Text="{Binding NotesInput, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,10,0"
                             materialDesign:HintAssist.Hint="ملاحظات"/>

                    <Button Grid.Column="3"
                            Style="{StaticResource AlAtmani.SoftButton}"
                            Height="45"
                            Content="إنشاء مرتجع"
                            Command="{Binding CreateReturnCommand}"/>
                </Grid>

                <!-- Filter Tabs -->
                <StackPanel Grid.Row="2"
                            Orientation="Horizontal"
                            Margin="0,0,0,15">
                    <Button Content="الكل"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Height="44"
                            Padding="20,10"
                            Margin="0,0,10,0"
                            Command="{Binding FilterAllCommand}"/>
                    <Button Content="قيد الانتظار"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Height="44"
                            Padding="20,10"
                            Margin="0,0,10,0"
                            Command="{Binding FilterPendingCommand}"/>
                    <Button Content="تم القبول"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Height="44"
                            Padding="20,10"
                            Margin="0,0,10,0"
                            Command="{Binding FilterApprovedCommand}"/>
                    <Button Content="مرفوض"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Height="44"
                            Padding="20,10"
                            Margin="0,0,10,0"
                            Command="{Binding FilterRejectedCommand}"/>
                    
                    <TextBox materialDesign:HintAssist.Hint="بحث برقم المرتجع أو الفاتورة أو العميل..."
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Padding="8,6"
                             Width="280"
                             Margin="20,0,0,0"
                             materialDesign:TextFieldAssist.HasClearButton="True"
                             VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Returns DataGrid -->
                <DataGrid Grid.Row="3"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          Background="Transparent"
                          BorderThickness="0"
                          HeadersVisibility="Column"
                          FontSize="14"
                          ItemsSource="{Binding Returns}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="رقم المرتجع"
                                            Binding="{Binding ReturnNumber}"
                                            Width="*"/>
                        <DataGridTextColumn Header="رقم الفاتورة"
                                            Binding="{Binding Sale.InvoiceNumber}"
                                            Width="*"/>
                        <DataGridTextColumn Header="اسم العميل"
                                            Binding="{Binding Customer.Name}"
                                            Width="2*"/>
                        <DataGridTextColumn Header="تاريخ المرتجع"
                                            Binding="{Binding ReturnDate, StringFormat={}{0:yyyy-MM-dd}}"
                                            Width="*"/>
                        <DataGridTextColumn Header="السبب"
                                            Binding="{Binding Reason}"
                                            Width="*"/>
                        <DataGridTextColumn Header="المبلغ"
                                            Binding="{Binding TotalAmount, StringFormat={}{0:N2} ج.م}"
                                            Width="*"/>
                        <DataGridTemplateColumn Header="الحالة" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Padding="12,4" CornerRadius="12" HorizontalAlignment="Left" VerticalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#F3F4F6"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Pending">
                                                        <Setter Property="Background" Value="#FEF3C7"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Approved">
                                                        <Setter Property="Background" Value="#DCFCE7"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Rejected">
                                                        <Setter Property="Background" Value="#FEE2E2"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock FontWeight="SemiBold" FontSize="12">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#4B5563"/>
                                                    <Setter Property="Text" Value="غير محدد"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Pending">
                                                            <Setter Property="Foreground" Value="#D97706"/>
                                                            <Setter Property="Text" Value="قيد الانتظار"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Approved">
                                                            <Setter Property="Foreground" Value="#15803D"/>
                                                            <Setter Property="Text" Value="تم القبول"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Rejected">
                                                            <Setter Property="Foreground" Value="#B91C1C"/>
                                                            <Setter Property="Text" Value="مرفوض"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="إجراءات"
                                                Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                 <DataTemplate>
                                     <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                         <Button Style="{StaticResource MaterialDesignIconButton}"
                                                 ToolTip="قبول المرتجع"
                                                 Width="34" Height="34" Margin="2,0">
                                             <materialDesign:PackIcon Kind="Check" Width="20" Height="20" Foreground="#10B981"/>
                                             <Button.Command>
                                                 <Binding Path="DataContext.ApproveReturnCommand"
                                                          RelativeSource="{RelativeSource AncestorType=Page}"/>
                                             </Button.Command>
                                             <Button.CommandParameter>
                                                 <Binding/>
                                             </Button.CommandParameter>
                                         </Button>
                                         <Button Style="{StaticResource MaterialDesignIconButton}"
                                                 ToolTip="رفض المرتجع"
                                                 Width="34" Height="34" Margin="2,0">
                                             <materialDesign:PackIcon Kind="Close" Width="20" Height="20" Foreground="#FB7185"/>
                                             <Button.Command>
                                                 <Binding Path="DataContext.RejectReturnCommand"
                                                          RelativeSource="{RelativeSource AncestorType=Page}"/>
                                             </Button.Command>
                                             <Button.CommandParameter>
                                                 <Binding/>
                                             </Button.CommandParameter>
                                         </Button>
                                     </StackPanel>
                                 </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>
    </Grid>
</Page>
