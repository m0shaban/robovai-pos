<Page x:Class="SmartPOS.WPF.Views.LoyaltyPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      FlowDirection="RightToLeft"
      Loaded="Page_Loaded">

    <Grid Background="{DynamicResource AlAtmani.BackgroundBrush}"
          TextElement.Foreground="{DynamicResource AlAtmani.TextPrimaryBrush}"
          Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:Card Grid.Row="0"
                             Style="{StaticResource AlAtmani.GlassCard}"
                             Margin="0,0,0,24"
                             Padding="24">
            <DockPanel>
                <Border DockPanel.Dock="Right" Background="{DynamicResource AlAtmani.Surface2Brush}"
                        CornerRadius="12"
                        Padding="12"
                        Margin="0,0,16,0"
                        VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="StarCircle"
                                             Width="32"
                                             Height="32"
                                             Foreground="{DynamicResource AlAtmani.AccentBrush}"/>
                </Border>
                <StackPanel DockPanel.Dock="Right" VerticalAlignment="Center">
                    <TextBlock Text="{DynamicResource Loc_5c8353a3}"
                               Style="{StaticResource AlAtmani.HeaderText}"/>
                    <TextBlock Text="{DynamicResource Loc_1e9b4fd8}"
                               FontSize="14"
                               Opacity="0.85"
                               Foreground="{DynamicResource AlAtmani.TextSecondaryBrush}"
                               Margin="0,4,0,0"/>
                </StackPanel>
                
                <!-- Toolbar -->
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button Command="{Binding LoadCommand}"
                            Style="{StaticResource MaterialDesignIconButton}"
                            ToolTip="{DynamicResource Loc_fd4458ee}"
                            Foreground="{DynamicResource AlAtmani.AccentBrush}">
                        <materialDesign:PackIcon Kind="Refresh" Width="24" Height="24"/>
                    </Button>
                </StackPanel>
            </DockPanel>
        </materialDesign:Card>

        <!-- Loyalty Tiers -->
        <Grid Grid.Row="1"
              Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Bronze -->
            <materialDesign:Card Grid.Column="0"
                                 Style="{StaticResource AlAtmani.GlassCard}"
                                 Margin="0,0,8,0"
                                 Padding="20">
                <StackPanel>
                    <Border Background="{DynamicResource AlAtmani.Surface2Brush}"
                            CornerRadius="12"
                            Padding="10"
                            HorizontalAlignment="Left"
                            Margin="0,0,0,12">
                        <materialDesign:PackIcon Kind="Medal"
                                                 Width="32"
                                                 Height="32"
                                                 Foreground="{DynamicResource AlAtmani.AccentBrush}"/>
                    </Border>
                    <TextBlock Text="{DynamicResource Loc_23f60d9e}"
                               FontSize="14"
                               Opacity="0.7"/>
                    <TextBlock Text="{DynamicResource Loc_abcf83cd}"
                               FontSize="12"
                               Opacity="0.6"
                               Margin="0,4,0,0"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="{Binding BronzeCount}" FontSize="22" FontWeight="Bold"/>
                        <TextBlock Text=" "/>
                        <TextBlock Text="{DynamicResource Loc_CustomersCountSuffix}" FontSize="14" VerticalAlignment="Bottom" Margin="4,0,0,2" Opacity="0.8"/>
                    </StackPanel>
                </StackPanel>
            </materialDesign:Card>

            <!-- Silver -->
            <materialDesign:Card Grid.Column="1"
                                 Style="{StaticResource AlAtmani.GlassCard}"
                                 Margin="8,0,8,0"
                                 Padding="20">
                <StackPanel>
                    <Border Background="{DynamicResource AlAtmani.Surface2Brush}"
                            CornerRadius="12"
                            Padding="10"
                            HorizontalAlignment="Left"
                            Margin="0,0,0,12">
                        <materialDesign:PackIcon Kind="Medal"
                                                 Width="32"
                                                 Height="32"
                                                 Foreground="{DynamicResource AlAtmani.AccentBrush}"/>
                    </Border>
                    <TextBlock Text="{DynamicResource Loc_811d411b}"
                               FontSize="14"
                               Opacity="0.7"/>
                    <TextBlock Text="{DynamicResource Loc_3629343d}"
                               FontSize="12"
                               Opacity="0.6"
                               Margin="0,4,0,0"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="{Binding SilverCount}" FontSize="22" FontWeight="Bold"/>
                        <TextBlock Text=" "/>
                        <TextBlock Text="{DynamicResource Loc_CustomersCountSuffix}" FontSize="14" VerticalAlignment="Bottom" Margin="4,0,0,2" Opacity="0.8"/>
                    </StackPanel>
                </StackPanel>
            </materialDesign:Card>

            <!-- Gold -->
            <materialDesign:Card Grid.Column="2"
                                 Style="{StaticResource AlAtmani.GlassCard}"
                                 Margin="8,0,8,0"
                                 Padding="20">
                <StackPanel>
                    <Border Background="{DynamicResource AlAtmani.Surface2Brush}"
                            CornerRadius="12"
                            Padding="10"
                            HorizontalAlignment="Left"
                            Margin="0,0,0,12">
                        <materialDesign:PackIcon Kind="Medal"
                                                 Width="32"
                                                 Height="32"
                                                 Foreground="{DynamicResource AlAtmani.AccentBrush}"/>
                    </Border>
                    <TextBlock Text="{DynamicResource Loc_8403da41}"
                               FontSize="14"
                               Opacity="0.7"/>
                    <TextBlock Text="{DynamicResource Loc_729cbd0a}"
                               FontSize="12"
                               Opacity="0.6"
                               Margin="0,4,0,0"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="{Binding GoldCount}" FontSize="22" FontWeight="Bold"/>
                        <TextBlock Text=" "/>
                        <TextBlock Text="{DynamicResource Loc_CustomersCountSuffix}" FontSize="14" VerticalAlignment="Bottom" Margin="4,0,0,2" Opacity="0.8"/>
                    </StackPanel>
                </StackPanel>
            </materialDesign:Card>

            <!-- Platinum -->
            <materialDesign:Card Grid.Column="3"
                                 Style="{StaticResource AlAtmani.GlassCard}"
                                 Margin="8,0,0,0"
                                 Padding="20">
                <StackPanel>
                    <Border Background="{DynamicResource AlAtmani.Surface2Brush}"
                            CornerRadius="12"
                            Padding="10"
                            HorizontalAlignment="Left"
                            Margin="0,0,0,12">
                        <materialDesign:PackIcon Kind="Crown"
                                                 Width="32"
                                                 Height="32"
                                                 Foreground="{DynamicResource AlAtmani.AccentBrush}"/>
                    </Border>
                    <TextBlock Text="{DynamicResource Loc_b54bbfde}"
                               FontSize="14"
                               Opacity="0.7"/>
                    <TextBlock Text="{DynamicResource Loc_038b0981}"
                               FontSize="12"
                               Opacity="0.6"
                               Margin="0,4,0,0"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="{Binding PlatinumCount}" FontSize="22" FontWeight="Bold"/>
                        <TextBlock Text=" "/>
                        <TextBlock Text="{DynamicResource Loc_CustomersCountSuffix}" FontSize="14" VerticalAlignment="Bottom" Margin="4,0,0,2" Opacity="0.8"/>
                    </StackPanel>
                </StackPanel>
            </materialDesign:Card>
        </Grid>

        <!-- Customer Loyalty List -->
        <materialDesign:Card Grid.Row="2"
                             Style="{StaticResource AlAtmani.GlassCard}"
                             Margin="0"
                             Padding="24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,16">
                    <TextBlock Text="{DynamicResource Loc_99fb0016}"
                               Style="{StaticResource AlAtmani.SubHeaderText}"
                               VerticalAlignment="Center"/>
                               
                    <TextBox materialDesign:HintAssist.Hint="{DynamicResource Loc_h_cb84af16}"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Padding="8,6"
                             Width="250"
                             HorizontalAlignment="Left"
                             materialDesign:TextFieldAssist.HasClearButton="True"
                             VerticalAlignment="Center"/>
                </DockPanel>

                <!-- Action Row -->
                <Grid Grid.Row="1"
                      Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ComboBox Grid.Column="0"
                              ItemsSource="{Binding Customers}"
                              DisplayMemberPath="Name"
                              SelectedItem="{Binding SelectedCustomer}"
                              Height="45"
                              Margin="0,0,10,0"
                              materialDesign:HintAssist.Hint="{DynamicResource Loc_h_3480cb21}"/>

                    <TextBox Grid.Column="1"
                             Height="45"
                             Text="{Binding PointsInput, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="{DynamicResource Loc_h_b4b712ba}"
                             Margin="0,0,10,0"/>

                    <Button Grid.Column="2"
                            Style="{StaticResource AlAtmani.SoftButton}"
                            Height="45"
                            Content="{DynamicResource Loc_34ab3fd3}"
                            Command="{Binding AddPointsCommand}"
                            Margin="0,0,10,0"/>

                    <Button Grid.Column="3"
                            Style="{StaticResource AlAtmani.SoftButton}"
                            Height="45"
                            Content="{DynamicResource Loc_0515b5dc}"
                            Command="{Binding RedeemPointsCommand}"/>
                </Grid>

                <TextBox Grid.Row="2"
                         Height="45"
                         Text="{Binding NotesInput, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="{DynamicResource Loc_h_3c4208fe}"/>

                <!-- Customer List -->
                <DataGrid Grid.Row="3"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          Background="Transparent"
                          BorderThickness="0"
                          HeadersVisibility="Column"
                          FontSize="14"
                          ItemsSource="{Binding Loyalties}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{DynamicResource Loc_2b9848ef}"
                                            Binding="{Binding Customer.Name}"
                                            Width="2*"/>
                        <DataGridTextColumn Header="{DynamicResource Loc_0947ad57}"
                                            Binding="{Binding Customer.Phone}"
                                            Width="*"/>
                        <DataGridTextColumn Header="{DynamicResource Loc_2092f067}"
                                            Binding="{Binding Points}"
                                            Width="*"/>
                        <DataGridTextColumn Header="{DynamicResource Loc_fbdbdeed}"
                                            Binding="{Binding TotalPointsEarned}"
                                            Width="*"/>
                        <DataGridTemplateColumn Header="{DynamicResource Loc_838259ac}" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Padding="12,4" CornerRadius="12" HorizontalAlignment="Left" VerticalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#F3F4F6"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Tier}" Value="Bronze">
                                                        <Setter Property="Background" Value="#FEF08A"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Tier}" Value="Silver">
                                                        <Setter Property="Background" Value="#E5E7EB"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Tier}" Value="Gold">
                                                        <Setter Property="Background" Value="#FEF9C3"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Tier}" Value="Platinum">
                                                        <Setter Property="Background" Value="#E0E7FF"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Width="14" Height="14" VerticalAlignment="Center" Margin="0,0,4,0">
                                                <materialDesign:PackIcon.Style>
                                                    <Style TargetType="materialDesign:PackIcon">
                                                        <Setter Property="Kind" Value="Medal"/>
                                                        <Setter Property="Foreground" Value="#4B5563"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Tier}" Value="Bronze">
                                                                <Setter Property="Foreground" Value="#A16207"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Tier}" Value="Silver">
                                                                <Setter Property="Foreground" Value="#4B5563"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Tier}" Value="Gold">
                                                                <Setter Property="Foreground" Value="#CA8A04"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Tier}" Value="Platinum">
                                                                <Setter Property="Kind" Value="Crown"/>
                                                                <Setter Property="Foreground" Value="#4338CA"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </materialDesign:PackIcon.Style>
                                            </materialDesign:PackIcon>
                                            <TextBlock FontWeight="SemiBold" FontSize="12" VerticalAlignment="Center">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="#4B5563"/>
                                                        <Setter Property="Text" Value="غير محدد"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Tier}" Value="Bronze">
                                                                <Setter Property="Foreground" Value="#A16207"/>
                                                                <Setter Property="Text" Value="برونزي"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Tier}" Value="Silver">
                                                                <Setter Property="Foreground" Value="#4B5563"/>
                                                                <Setter Property="Text" Value="فضي"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Tier}" Value="Gold">
                                                                <Setter Property="Foreground" Value="#CA8A04"/>
                                                                <Setter Property="Text" Value="ذهبي"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Tier}" Value="Platinum">
                                                                <Setter Property="Foreground" Value="#4338CA"/>
                                                                <Setter Property="Text" Value="بلاتيني"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="{DynamicResource Loc_4041e780}"
                                            Binding="{Binding LastTierUpdate, StringFormat={}{0:yyyy-MM-dd}}"
                                            Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>
    </Grid>
</Page>
