# نظرة عامة على المشروع — RobovAI PRO POS

> نظرة صريحة وشاملة على المنظومة: ما هي، ولمن صُمِّمت، وما الذي جاهز فعلاً للإنتاج.

---

## ما هو النظام

**RobovAI PRO POS** منصة متكاملة لنقاط البيع وإدارة المخزون، تتكوّن من منتجَين مستقلَّين قابلَين للتكامل:

| المكوّن                     | التقنية                            | المنصة               | طريقة النشر                            |
| --------------------------- | ---------------------------------- | -------------------- | -------------------------------------- |
| **SmartPOS WPF**            | C# .NET 8، WPF، EF Core / SQLite   | سطح المكتب (Windows) | مُثبِّت InnoSetup (`.exe`)             |
| **Smart Inventory Pro WMS** | Vanilla JS PWA، Firebase، Dexie.js | أي متصفح (PWA)       | GitHub Pages — `pos.robovai.tech/wms/` |

---

## حالات الاستخدام

### 1. WPF بمفرده — كاشير محلي بلا إنترنت

**المستخدم المستهدف**: صاحب متجر صغير، شباك دفع واحد، لا يحتاج إنترنت.

**ما يفعله**:

- دورة بيع كاملة: إضافة منتج ← حساب الإجمالي ← استلام نقداً أو بطاقة ← طباعة الفاتورة
- قاعدة بيانات SQLite محلية (بدون أي اعتماد على السحابة)
- إدارة كتالوج المنتجات والتصنيفات الهرمية
- تقارير مبيعات أساسية مع رسوم بيانية (LiveCharts)
- سجل مشتريات العملاء
- مسح الباركود عبر قارئ USB أو الكاميرا (OpenCvSharp4)
- واجهة عربية وإنجليزية، تخطيط RTL عبر MaterialDesign

**النشر**: مُثبِّت `.exe` واحد، اكتفائي ذاتياً، يعمل على Windows 10/11 x64، لا يحتاج تثبيت .NET مسبقاً.

**الإصدارات المتاحة**:

- **Platinum** — مميزات كاملة، علامة تجارية راقية (`wizard_small.bmp`)
- **Kaf5** — نسخة مُخصَّصة لعميل بعينه (`wizard_small_kaf5.bmp`)

---

### 2. WMS بمفرده — PWA للمخزون والمستودع

**المستخدم المستهدف**: مشرف مستودع أو فريق متعدد يحتاج وصولاً عبر المتصفح.

**ما يفعله**:

- واجهة عربية RTL بالكامل، تعمل بلا إنترنت (IndexedDB عبر Dexie.js)
- منتجات، تصنيفات، موردون، عملاء، أوامر شراء، فواتير
- تسويات مخزون، حركات بضاعة، تنبيهات نفاد المخزون
- تتبع المصروفات
- لوحة تحكم مع رسوم بيانية (Chart.js)
- تصدير PDF (jsPDF + autotable)، تصدير Excel (xlsx)، توليد باركود (JsBarcode)
- إدارة المستخدمين بأدوار (مدير / مشرف / عامل)
- مزامنة Firebase Firestore للتعاون الفوري متعدد المستخدمين
- قابل للتثبيت كـ PWA (Service Worker + Manifest)

**النشر**: مستضاف على GitHub Pages، متاح من أي جهاز عبر `pos.robovai.tech/wms/`.

---

### 3. WPF + WMS معاً — المنظومة الكاملة

**المستخدم المستهدف**: تاجر لديه كاشير ثابت على الحاسوب وفريق يدير المخزون من أجهزة لوحية أو هواتف.

**آلية الربط — إقران QR للمصادقة**:

1. المدير في WMS يفتح الإعدادات ← إدارة المستخدمين ← يضغط 🔲 بجانب مستخدم
2. WMS يولّد حمولة QR موقَّعة (`qr-auth-v1`، HMAC-SHA256)
3. كاشير WPF يفتح تسجيل الدخول ← يختار "تسجيل بـ QR" ← يوجّه الكاميرا نحو الشاشة
4. WPF يفكّ رمز QR، يتحقق من توقيع HMAC، ويسجّل دخول المستخدم المطابق

**ما يتيحه هذا**:

- WMS هو دليل المستخدمين؛ WPF يصادق عبر QR دون إدخال كلمة مرور
- الكاشير يعمل offline على WPF؛ المدير يراقب المخزون من المتصفح
- مستقبلاً: مزامنة ثنائية الاتجاه بين مبيعات WPF ومخزون WMS عبر Firebase

---

## نموذج العمل التجاري

| الجانب               | الوضع الحالي                                                       |
| -------------------- | ------------------------------------------------------------------ |
| **نموذج الترخيص**    | بيع مرة واحدة لكل تثبيت (لا اشتراك شهري)                           |
| **طريقة التسليم**    | مُثبِّت `.exe` يُسلَّم مباشرة للعميل                               |
| **الإصدارات**        | Platinum (كامل المميزات) ونسخ مُبرمَجة لعملاء بعينهم               |
| **استضافة WMS**      | مجانية — GitHub Pages + Firebase Spark                             |
| **الدعم والتحديثات** | يدوي — مُثبِّت جديد يُسلَّم بواسطة المطوّر                         |
| **السوق المستهدف**   | الشركات الصغيرة والمتوسطة الناطقة بالعربية (الخليج + مصر)          |
| **مصدر الإيراد**     | نسخ مُخصَّصة لكل عميل + البيع الأولي؛ لا نظام فوترة متكرر حتى الآن |

---

## جاهزية الإنتاج — تقييم صريح

### ✅ ما هو جاهز للإنتاج

| الجانب                      | الحالة                                                |
| --------------------------- | ----------------------------------------------------- |
| WPF كاشير offline           | جاهز — SQLite يحتفظ بالبيانات، لا اعتماد على الشبكة   |
| WMS PWA offline             | جاهز — Dexie.js IndexedDB + Service Worker            |
| مزامنة WMS Firebase         | جاهز للفرق الصغيرة — Firestore في الوقت الفعلي        |
| إقران QR للمصادقة           | مُصلَح ومنشور — WPF deeplink + توليد QR موقَّع من WMS |
| واجهة عربية RTL             | كلا المنتجين عربيان بالكامل                           |
| المُثبِّت (Platinum & Kaf5) | جاهز — InnoSetup v6.7.1، اكتفائي ذاتي                 |
| صفحة الهبوط                 | مباشرة على `pos.robovai.tech` عبر GitHub Pages        |

---

### ⚠️ ما هو ناقص أو هشّ

| الجانب                             | المشكلة                                                                                                                                                             |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **المزامنة الثنائية WPF ↔ WMS**    | لا توجد مزامنة بيانات ثنائية الاتجاه. WPF يكتب محلياً في SQLite؛ WMS يكتب في Firebase. لا جسر بينهما سوى تسجيل الدخول بـ QR. عملية بيع في WPF لا تُخفِّض مخزون WMS. |
| **معالجة الدفع في WPF**            | الإجماليات نقداً وبطاقة تُحسَب، لكن لا يوجد تكامل حقيقي مع جهاز POS (لا SDK، لا API لجهة الاستحواذ).                                                                |
| **مصادقة مستخدمي WMS**             | Firebase Auth للدخول؛ نظام الأدوار عبر QR تطبيقي فقط وغير مفروض في قواعد Firestore لكل مجموعة.                                                                      |
| **قواعد أمان Firestore**           | لا دليل على قواعد Firestore مُحكَمة تفرض عزل البيانات بين المستخدمين. مستخدم متحيّل قد يقرأ بيانات مستأجر آخر.                                                      |
| **تعددية المستأجرين**              | WMS مستأجر واحد فعلياً لكل مشروع Firebase. عملاء مختلفون يشتركون في نفس قاعدة Firestore ما لم تُنشأ مشاريع منفصلة.                                                  |
| **النسخ الاحتياطي السحابي لـ WPF** | قاعدة SQLite موجودة فقط على جهاز الكاشير. لا آلية نسخ احتياطي أو استرداد عن بُعد.                                                                                   |
| **طابعة الفواتير في WPF**          | كود توليد الفاتورة موجود لكن التكامل أساسي — لا مشغّل ESC/POS للطابعات الحرارية، فقط نافذة طباعة Windows العامة.                                                    |
| **فرض الترخيص/التفعيل**            | سكريبت توليد المفتاح (`generate_license_key.py`) موجود، لكن لا فحص ترخيص حقيقي في وقت تشغيل WPF. المُثبِّت غير مربوط بمعرّف الجهاز.                                 |
| **مراقبة الأخطاء**                 | لا تقارير أعطال (لا Sentry، لا Application Insights). الأعطال على أجهزة العملاء غير مرئية.                                                                          |
| **الاختبارات الآلية**              | تغطية اختبارات شبه معدومة في كلا المنتجين. Playwright موجود كـ dev dependency في WMS لكن لا ملفات اختبار مكتوبة.                                                    |
| **CI/CD**                          | لا خط بناء آلي. البناء يدوي (npm run build → نسخ → git push لـ WMS؛ dotnet publish + ISCC.exe لـ WPF).                                                              |
| **الترجمة**                        | الواجهة عربية أولاً؛ نصوص إنجليزية موجودة في أجزاء من WPF لكن التطبيق ليس ثنائي اللغة فعلياً عبر نظام i18n منظَّم.                                                  |

---

### 🔴 مخاطر أمنية تستوجب المعالجة قبل التوزيع الواسع

| الخطر                           | التفاصيل                                                                                                                                                                               |
| ------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **سر HMAC للـ QR**              | المفتاح السري لمصادقة QR مخزَّن في `localStorage` (`robovai_qr_secret`). إذا تمكّن مهاجم من قراءة localStorage (عبر XSS أو وصول مادي)، يستطيع تزوير رمز QR لأي مستخدم.                 |
| **مفتاح Firebase API في الكود** | `AIzaSyDO9Z2GppWCMVx4QuFTEJyUBXBfsZ5v7xQ` مُضمَّن بشكل ثابت في حزمة WMS وظاهر لأي مستخدم. هذا مقبول لتطبيقات Firebase على الويب لكنه يستلزم قواعد Firestore + Auth صارمة لضمان الأمان. |
| **لا تحديد معدل لمسح QR**       | تسجيل الدخول بـ QR في WPF لا يحتوي على حماية من القوة الغاشمة أو إعادة التشغيل. رمز QR مُلتقَط يبقى صالحاً حتى تتغير المفاتيح.                                                         |

---

## ملخص التقنيات

**SmartPOS WPF** (`src/SmartPOS.WPF/`)

- .NET 8، WPF، Windows 10.0.19041+، x64 فقط
- MaterialDesign 5.3.2، CommunityToolkit.Mvvm، EF Core 8 / SQLite
- LiveCharts (SkiaSharp)، OpenCvSharp4، QRCoder
- معمارية نظيفة: `Core` / `Application` / `Infrastructure` / `WPF`

**Smart Inventory Pro WMS** (`smart-inventory-pro/`)

- Vite 6.4.2، Vanilla JS (ES Modules، بدون إطار عمل)
- Firebase 12 (Firestore + Auth)، Dexie.js 4 (IndexedDB)
- Chart.js 4، jsPDF 3، xlsx 0.18، SweetAlert2 11، أيقونات Lucide
- الحزمة المنشورة: بناء Vite بمدخل واحد ← `~2.85 MB` JS
- الإصدار: 2.6.0
